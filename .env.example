# Public site URL (sitemap, canonical links, llms.txt)
NEXT_PUBLIC_SITE_URL=https://dzpartpicker.dz

# Supabase — live catalog (tables: stores, canonical_products, offers, price_history)
# Get these from: Supabase dashboard → Project Settings → API
# (anon/publishable key is public by design; reads go through public-read RLS)
NEXT_PUBLIC_SUPABASE_URL=https://sculmoqvdfcohnvtsrsb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=[SECURITY_DATA]

# Admin console (/admin-kh7?key=...) — set a strong secret in production
ADMIN_KEY=[SECURITY_DATA]

# Vercel cron (/api/cron/refresh) — set a strong secret in production
CRON_SECRET=[SECURITY_DATA]
